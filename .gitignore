# Đánh dấu "máy này là máy viết skill" — do `node scripts/setup.mjs --dev` tạo ra.
# Theo máy, không theo repo, nên không commit.
.sonny-dev

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.idea/
node_modules/
